server:
  port: 8888

spring:
  application:
    name: helloworld-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always